{
  "name": "illustrator-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first MCP control layer for Adobe Illustrator with accuracy-focused SVG tracing intelligence.",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "svg-path-parser": "^1.1.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
